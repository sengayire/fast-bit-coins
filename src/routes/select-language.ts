import { SELECT_LANGUAGE_SCREEN } from 'src/constants/screens'
import component from 'src/screens/select-language'

// const component = import('src/screens/select-language')

export default {
  name: SELECT_LANGUAGE_SCREEN,
  title: SELECT_LANGUAGE_SCREEN,
  ShownHeader: false,
  component
}
