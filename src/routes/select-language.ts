import { SELECT_LANGUAGE_SCREEN_NAME } from 'src/constants/screens'
import component from 'src/screens/select-language'

export default {
  name: SELECT_LANGUAGE_SCREEN_NAME,
  title: '',
  shownHeader: false,
  component,
  protected: false
}
