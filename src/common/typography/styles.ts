import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  text: {
    paddingHorizontal: 48,
    paddingVertical: 19,
    textAlign: 'center',
    fontSize: 14,
    lineHeight: 18,
    fontWeight: 400
  }
})
